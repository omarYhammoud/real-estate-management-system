// Shared front-end JS. Keep app-specific scripts in the app's own
// static/<app_name>/ directory once you need them.
document.addEventListener('DOMContentLoaded', () => {
  console.log('Real Estate Management System loaded.');
});
